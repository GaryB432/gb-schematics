{
  "$schema": "http://json-schema.org/schema",
  "id": "GbAngularIISConfig",
  "title": "Angular IIS Config Options Schema",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "visible": false,
      "$default": {
        "$source": "projectName"
      }
    }
  },
  "required": []
}
