import assert from 'node:assert/strict';
import { describe, it } from 'node:test';

import { add } from './oven.js';

describe('oven', () => {
	it('should add', () => {
		assert.equal(add(3, 2, 1), 1 + 2 + 3);
	});
});
